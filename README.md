# Flask Application

## Run locally

```sh
python3 -m venv .venv
source .venv/bin/activate  # on Windows: `.venv\Scripts\activate`
pip install -r requirements.txt  # or simply `pip install Flask`
flask --app app run --debug.     # or simply `python app.py`
```

Open <http://127.0.0.1:5000>.
